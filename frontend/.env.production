# Production build config (used by `vite build`).
# The frontend is served by the Express backend from the same origin, so the
# API is reached at a relative "/api" path — no absolute URL, no CORS.
VITE_API_URL=/api
